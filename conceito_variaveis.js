// var, const e let
// 1-const (constante)
const pi = 3.14
// imprimindo a variável no console 
//console.log(pi);


// 2 - let (o valor da variável pode ser alterado)
let nome  = 'Micaelly'
console.log(nome)

nome = 'Milla';
console.log(nome);

let desconto = 20.5
// multiplicando a variável , sem alterar o valor original
console.log(desconto * 2)

// atualizando o valor da variavel utilizando a própria variável
desconto = desconto * 2;
console.log('Desconto atualizado: ' + desconto);

// Array (lista)
let tamanhos = ['PP', 'P', 'M', 'G'];

let temPet = true;

const idade = 22;

let dataCadastro = new Date(); //pega a data e hora atual
console.log(dataCadastro);
